# Production Environment Configuration
# CRITICAL: Never commit this file with real values

# Database - Use strong credentials
DATABASE_URL=postgresql://username:strong_password@localhost:5432/retail_analytics
POSTGRES_DB=retail_analytics
POSTGRES_USER=analytics_user
POSTGRES_PASSWORD=generate_strong_password_here

# Security - Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your_32_character_secret_key_here

# CORS - Only your production domains
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Environment
ENVIRONMENT=production
DEBUG=false

# Optional: Google Sheets
GOOGLE_CREDENTIALS_FILE=/path/to/production/credentials.json

# Monitoring (future)
# SENTRY_DSN=your_sentry_dsn_here
# LOG_LEVEL=INFO